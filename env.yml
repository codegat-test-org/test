name: payu
channels:
  - accessnri
  - coecms
  - conda-forge
dependencies:
  # note that the deployment requires a specific version of 'accessnri::payu' to run
  - accessnri::payu
  - coecms::f90nml
  - conda-lock
  - conda-pack
  - gh
  - git
 
