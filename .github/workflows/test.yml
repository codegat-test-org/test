on:
  workflow_dispatch:
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: output1
        run: |
          ./scripts/send-output.bash

      - id: output2
        run: |
          ./scripts/send-output.bash

      - run: |
          echo "the output was ${{ steps.output1.outputs.something }} and ${{ steps.output2.outputs.something }}"
