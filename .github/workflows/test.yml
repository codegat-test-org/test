name: Test env stuff
on:
  workflow_dispatch:
jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - run: |
          echo "Wow ${{ github.actor }} what a comment!" > ./file.txt
          cat ./file.txt
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        uses: access-nri/actions/.github/actions/pr-comment@pr-comment-allow-file
        with:
          comment: Hello
          pr: 26
