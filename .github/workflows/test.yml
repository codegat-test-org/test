name: Test
on:
  workflow_dispatch:
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TOKENS=($(head -n 1 <<< "some bunch of commands"))" >> $GITHUB_ENV

      - run: echo "${TOKENS[1]}"