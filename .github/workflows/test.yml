on:
  workflow_dispatch:
jobs:
  mv-stuff:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - run: |
          mkdir testing
          mv -v src/*/* testing
          tree