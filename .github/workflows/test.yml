name: Test Draft Steps
on:
  workflow_dispatch:
jobs:
  bools:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat <<EOF >> my.file
          repos::
            - something
            - $spack/else
          EOF

          cat my.file