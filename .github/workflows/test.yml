name: Test Jinja Generation
on:
  workflow_dispatch:
jobs:
  join-test:
    runs-on: ubuntu-latest
    steps:
      - id: generate
        run: echo "list='One Two Three Four Five'" >> $GITHUB_OUTPUT

      - run: echo "${{ join(steps.generate.outputs.list, '\n') }}"
