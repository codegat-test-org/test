name: Test Draft Steps
on:
  workflow_dispatch:
jobs:
  container:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/access-nri/base-spack-intel2021.2.0-2025.03.005:latest
      env:
        MY_ENV: "my env"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: env

      - run: |
          echo "Via gha ${{ env.SHLVL }}"
          echo "Via env $SHLVL"
          echo "my env is ${{ env.MY_ENV }}"
          echo "or $MY_ENV"

      - if: env.SHLVL == '2'
        run: echo "SHLVL is 2"

      - if: env.MY_ENV == 'my env'
        run: echo "MY_ENV is my env"
