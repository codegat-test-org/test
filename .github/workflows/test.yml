name: Test
on:
  workflow_dispatch:
jobs:
  var1:
    runs-on: ubuntu-latest
    env:
      VAR: Release
    steps:
      - run: echo "Hello ${{ env.VAR != 'Release' && env.VAR }}"
  var2:
    runs-on: ubuntu-latest
    env:
      VAR: Prerelease
    steps:
      - run: echo "Hello ${{ env.VAR != 'Release' && env.VAR }}"
