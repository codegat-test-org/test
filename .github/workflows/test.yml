name: Test updating commit status
on:
  pull_request:
  issue_comment:
    types:
      - created
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    permissions:
      statuses: write
    steps:
      - run: echo "${{ fromJson(github.event.issue) }}"
      - if: ${{ ! github.event.pull_request.draft || ! github.event.issue.draft }}
        run: |
          echo "wooo"