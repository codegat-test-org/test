name: Test
on:
  workflow_dispatch:
jobs:
  join-string-test:
    runs-on: ubuntu-latest
    steps:
      - id: targets
        uses: access-nri/build-cd/.github/actions/get-target-matrix@209-support-deployment-target-choice
        with:
          targets: "Gadi Setonix"

      - run: echo "${{ steps.targets.outputs.valid-targets }}"