name: Test
on:
  workflow_dispatch:
jobs:
  var1:
    runs-on: ubuntu-latest
    steps:
      - id: t
        uses: access-nri/build-cd/.github/actions/get-target-matrix@dev-121-multi-target-workflows_TEST
        with:
          targets: GadiTest

      - run: echo "${{ steps.t.outputs.valid-targets }}"
